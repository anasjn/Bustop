<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.pfc.android.bustop.ScrollingActivity"
    tools:showIn="@layout/activity_scrolling">

    <ScrollView
        android:id="@+id/content_scrolling"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/firsttime"
                style="@style/RestItemStyle"
                android:text="@string/timing1_text" />

            <TextView
                android:id="@+id/incidents_title"
                style="@style/FirstItemStyle"
                android:text="@string/incident_tittle" />

            <TextView
                android:id="@+id/incidents"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@string/large_text" />
        </LinearLayout>
    </ScrollView>

</android.support.v4.widget.NestedScrollView>
